import Foundation

/// Represents a directional arrow for UI navigation and window positioning
enum ArrowDirection {
    /// Upward direction (↑)
    case up
    
    /// Downward direction (↓)
    case down
    
    /// Leftward direction (←)
    case left
    
    /// Rightward direction (→)
    case right
} 